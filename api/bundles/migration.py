async def migrate_settings_by_bundle(
    source_bundle: str,
    target_bundle: str,
    source_variant: str,
    target_variant: str,
    with_projects: bool = True,
) -> None:
    pass  # TODO: do the magic here
